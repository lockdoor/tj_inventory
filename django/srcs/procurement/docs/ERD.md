# Procurement Domain ERD

This document outlines the database schema for the Procurement module, focusing on Purchase Orders, Arrivals, Reservations, and Shortage handling.

```mermaid
erDiagram
    PURCHASE_ORDER ||--o{ PURCHASE_ORDER_ITEM : contains
    PURCHASE_ORDER ||--o{ ARRIVAL : "receives as"
    ARRIVAL ||--o{ ARRIVAL_ITEM : contains
    
    RESERVATION ||--o{ RESERVATION_ITEM : contains
    RESERVATION_ITEM ||--o{ RESERVATION_ALLOCATION : "allocated from"
    RESERVATION_ITEM ||--o{ SHORTAGE : "may generate"
    SHORTAGE ||--o{ PURCHASE_ORDER_ITEM : "fulfilled by"
    
    STOCK_LOT ||--o{ RESERVATION_ALLOCATION : "provides physical stock"
    ARRIVAL_ITEM ||--o{ RESERVATION_ALLOCATION : "provides incoming stock"

    PURCHASE_ORDER {
        string document_no PK
        string partner_code FK "Supplier"
        date expected_date
        string status "DRAFT, SUBMITTED, PARTIAL, COMPLETED, CANCELLED"
        text note
        datetime created_at
    }

    PURCHASE_ORDER_ITEM {
        int id PK
        string po_no FK
        string item_sku FK
        decimal order_qty
        decimal unit_cost
    }

    ARRIVAL {
        string document_no PK
        string po_no FK "Optional - Linked to PO"
        date expected_date
        string status "SCHEDULED, IN_TRANSIT, RECEIVED, CANCELLED"
        string partner_code FK
        string warehouse_code FK
        text note
        datetime created_at
    }

    ARRIVAL_ITEM {
        int id PK
        string arrival_no FK
        string item_sku FK
        decimal expected_qty
        decimal received_qty
        decimal unit_cost
    }

    RESERVATION {
        string document_no PK
        string store_code FK "Initiating Store"
        string status "DRAFT, ACTIVE, PARTIAL, FULFILLED, CANCELLED"
        string reference_type "STORE_ORDER, PRODUCTION, OTHER"
        string reference_no
        datetime created_at
    }

    RESERVATION_ITEM {
        int id PK
        string reservation_no FK
        string item_sku FK
        decimal requested_qty
        datetime expiry_date
    }

    RESERVATION_ALLOCATION {
        int id PK
        int reservation_item_id FK
        string source_type "LOT, ARRIVAL"
        string lot_id FK "If allocated from physical stock"
        int arrival_item_id FK "If allocated from incoming arrival"
        decimal allocated_qty
    }

    SHORTAGE {
        int id PK
        int reservation_item_id FK
        string item_sku FK
        decimal shortage_qty
        string status "PENDING, PO_CREATED, RESOLVED, CANCELLED"
        datetime created_at
    }

    STOCK_LOT {
        string lot_id PK
        string item_sku FK
        string warehouse_code FK
        decimal available_qty
    }

    %% External Relationships
    PARTNER ||--o{ PURCHASE_ORDER : "supplies"
    PARTNER ||--o{ ARRIVAL : "ships to"
    WAREHOUSE ||--o{ ARRIVAL : "receives"
    ITEM ||--o{ ARRIVAL_ITEM : "listed in"
    ITEM ||--o{ PURCHASE_ORDER_ITEM : "listed in"
    ITEM ||--o{ RESERVATION_ITEM : "reserved"
    ITEM ||--o{ STOCK_LOT : "tracked in"
```

## Entity Descriptions

### 1. Reservation & ReservationItem
Operates like a "shopping cart". The `Reservation` is the main document (the cart) created by the physical store. It contains multiple `ReservationItem` records (the products in the cart), each representing a request for a specific product and quantity.

### 2. ReservationAllocation
Links the requested quantity to actual supply. An item can be allocated against:
- **Physical Stock (`STOCK_LOT`)**: Items already in the warehouse with `available_qty > 0`.
- **Incoming Stock (`ARRIVAL_ITEM`)**: Items that have been ordered from a supplier and are scheduled to arrive soon.

### 3. Shortage
If the `requested_qty` of a `ReservationItem` cannot be fully met by allocations from `STOCK_LOT` or `ARRIVAL_ITEM`, the system generates a `Shortage` record for the remaining deficit. The Stock Controller monitors these records to group them and create `Purchase Orders`.

### 4. Purchase Order (PO)
The formal request sent to a supplier (Partner) to procure stock. Generated by the Stock Controller to cover identified `Shortage` records or for general inventory replenishment.

### 5. Arrival
Represents a planned or actual shipment from a supplier. It can be linked to a `Purchase Order` and serves as the "Intent" document that will eventually trigger an `InventoryMovement` upon physical receipt.

### 6. ArrivalItem & PurchaseOrderItem
Line items detailing specific products. `ArrivalItem` tracks `expected_qty` versus `received_qty` to easily handle partial deliveries against the original PO.
